/**
 * Barrel dos services de DOMÍNIO (blueprint §1). A infra — `apiClient`, `apiErrors`,
 * `apiBase`, `supabase` — fica de fora de propósito; importe-a direto quando precisar.
 */
export * from "@/services/userService"
